# GPU LoRA/QLoRA style finetuning dependencies.
# Install a CUDA build of PyTorch first if needed:
#   https://pytorch.org/get-started/locally/
# Then run: pip install -r requirements.txt

torch>=2.2
transformers>=4.45.0
datasets>=2.19.0
accelerate>=0.31.0
peft>=0.11.0
bitsandbytes>=0.43.0
safetensors>=0.4.3
PyYAML>=6.0.1
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0
